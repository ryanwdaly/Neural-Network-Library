
class NeuralNetwork {
    construnctor(numI, numH, numO) {
        this.inputNodes = numI;
        this.hiddenNodes = numH;
        this.outputNodes = numO;
    }
    feedforward(input) {
        let guess;

        // Matrix Math

        return guess
    }
}